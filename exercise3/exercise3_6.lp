#show groupMinus/2.
#show universityMinus/1.
% Model: empty.

% mnr: 1
% incs: groupMinus(parcomp,100)
% incs: groupMinus(complang,100)
% incs: groupMinus(logic,100)
% incs: groupMinus(datascience,100)
% incs: groupMinus(compilers,100)
% incs: groupMinus(ds,100)
% incs: groupMinus(ml,100)
% incs: groupMinus(ai,100)
% incs: groupMinus(business,100)
% incs: groupMinus(algo,100)
% incs: universityMinus(800)

person(albert, 100).
person(bernd, 100).
person(clarissa, 100).
person(dorian, 100).
person(emil, 100).
person(flora, 100).
person(gustav, 100).
person(heinrich, 100).
person(ida, 100).
person(jakob, 100).

person(klaus, 100).
person(laura, 100).

assigned(albert, parcomp).
assigned(bernd, complang).
assigned(clarissa, logic).
assigned(dorian, datascience).
assigned(emil, compilers).
assigned(flora, ds).
assigned(gustav, ml).
assigned(heinrich, ai).
assigned(ida, business).
assigned(jakob, algo).